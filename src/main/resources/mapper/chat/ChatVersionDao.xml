<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tmesh.im.app.chat.dao.ChatVersionDao">

    <!-- 字段集 -->
    <sql id="baseSql">
        select id, version, url, content from chat_version
    </sql>

    <!-- 查询列表字段 -->
    <select id="queryList" resultType="com.tmesh.im.app.chat.domain.ChatVersion">
        <include refid="baseSql"/>
        <where>

        </where>
    </select>

</mapper>
